# Salon Frontend

Web client for the Salon Management System.

This workspace is part of the monorepo. The UI has not been scaffolded yet. When frontend work starts, the app will live entirely under `frontend/` and talk to the API at `/api/v1`.
